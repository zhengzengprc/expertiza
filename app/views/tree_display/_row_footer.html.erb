<% 
   if child_nodes 
   index = 1 
   for node in child_nodes
%>

<!-- Start Listing -->
<% if node != nil%>
	<%= render :partial=> '/student_task/entry', :locals => {:node => node, :prefix => prefix+"_"+index.to_s, :depth => depth + 1, :rowtype => rowtype } %>
<% end %>
<!-- End Listing -->
<% 
   index+=1   
   end 
%>
<% end %>
